resources:
  - name: dataworks-batch-job-handlers-pr
    type: pull-request
    source:
      repository: dwp/dataworks-batch-job-handlers
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: dataworks-batch-job-handlers
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/dataworks-batch-job-handlers.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h
