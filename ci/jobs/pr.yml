jobs:
  - name: dataworks-batch-job-handlers-pr
    plan:
      - get: dataworks-batch-job-handlers-pr
        trigger: true
        version: every
      - put: dataworks-batch-job-handlers-pr
        params:
          path: dataworks-batch-job-handlers-pr
          status: pending
      - .: (( inject meta.plan.terraform-bootstrap ))
        input_mapping:
          dataworks-batch-job-handlers: dataworks-batch-job-handlers-pr
      - .: (( inject meta.plan.terraform-plan ))
        input_mapping:
          dataworks-batch-job-handlers: dataworks-batch-job-handlers-pr
        params:
          TF_WORKSPACE: "management"
          DETAILED_EXITCODE: ""
        on_failure:
          put: dataworks-batch-job-handlers-pr
          params:
            path: dataworks-batch-job-handlers-pr
            status: failure
        on_success:
          put: dataworks-batch-job-handlers-pr
          params:
            path: dataworks-batch-job-handlers-pr
            status: success
