groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - dataworks-batch-job-handlers-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-dataworks-batch-job-handlers-dev
      - mirror-dwpdigital-dataworks-batch-job-handlers
